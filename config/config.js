// speedSeconds = How many seconds will a single item be shown on screen
// delaySeconds = How many seconds will the app be inactive before looping through again
// titleColor = Color of top text in hex, rgb, or alias (name of social media)
// textColor = Color of bottom text in hex, rgb, or alias (name of your handle)

const json = {
  speedSeconds: 5,
  delaySeconds: 0,
  titleColor: "white",
  textColor: "white",
};

export default json;
